Place the NTI logo image here as PNG:

Save the provided logo attachment as:

  public/logo/NTILogo.png

The application layout references this path to show the logo in the top-left of the site.

If you already uploaded the image elsewhere, copy it to the above path or update `app/layout.tsx` to point to the correct URL.
